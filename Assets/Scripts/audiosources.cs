using UnityEngine;
using System; // Needed for the [Serializable] attribute
[Serializable]
    public class audiosources
    {
        public AudioClip[] audioClips;
    }

    // 2. Create a public array (or List) of that custom class
